<Page x:Class="YP.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:YP.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#ecf0f1" Margin="5">
            <Button x:Name="AuthBtn" Content="Войти" 
                HorizontalAlignment="Right" VerticalAlignment="Center" Width="60" Height="24" Margin="3" Click="AuthBtn_Click"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Background="#2c3e50">
                <Button Width="40" Height="40" Margin="10" ToolTip="Каталог книг" Click="CatalogBtn_Click">
                    <Image Source="/Images/book.png" Width="24" Height="24"/>
                </Button>
                <Button Width="40" Height="40" Margin="10" ToolTip="Мои списки" Click="ListsBtn_Click">
                    <Image Source="/Images/clipboard.png" Width="24" Height="24"/>
                </Button>
                <Button x:Name="adminBtn" Width="40" Height="40" Margin="10" ToolTip="Администрирование" Click="AdminBtn_Click" Visibility="Collapsed">
                    <Image Source="/Images/administrative.png" Width="24" Height="24"/>
                </Button>
                <Button x:Name="authorBtn" Width="40" Height="40" Margin="10" ToolTip="Мои книги" Click="AuthorBtn_Click" Visibility="Collapsed">
                    <Image Source="/Images/feather.png" Width="24" Height="24"/>
                </Button>
                <Button Width="40" Height="40" Margin="10" ToolTip="Профиль" Click="ProfileBtn_Click">
                    <Image Source="/Images/user.png" Width="24" Height="24"/>
                </Button>
                <Button Content="Выйти" Width="40" Height="40" Margin="10" 
                    ToolTip="Выйти" Click="LogoutBtn_Click"/>
            </StackPanel>

            <Frame x:Name="ContentFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>
